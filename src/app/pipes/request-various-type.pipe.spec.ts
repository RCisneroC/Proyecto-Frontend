import { RequestVariousTypePipe } from './request-various-type.pipe';

describe('RequestVariousTypePipe', () => {
  it('create an instance', () => {
    const pipe = new RequestVariousTypePipe();
    expect(pipe).toBeTruthy();
  });
});
