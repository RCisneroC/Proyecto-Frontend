import { RequestVariousTypeIdPipe } from './request-various-type-id.pipe';

describe('RequestVariousTypeIdPipe', () => {
  it('create an instance', () => {
    const pipe = new RequestVariousTypeIdPipe();
    expect(pipe).toBeTruthy();
  });
});
