import { StatusTeacherPipe } from './status-teacher.pipe';

describe('StatusTeacherPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusTeacherPipe();
    expect(pipe).toBeTruthy();
  });
});
