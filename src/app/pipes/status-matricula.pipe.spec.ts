import { StatusMatriculaPipe } from './status-matricula.pipe';

describe('StatusMatriculaPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusMatriculaPipe();
    expect(pipe).toBeTruthy();
  });
});
