import { VerificarBS64Pipe } from './verificar-bs64.pipe';

describe('VerificarBS64Pipe', () => {
  it('create an instance', () => {
    const pipe = new VerificarBS64Pipe();
    expect(pipe).toBeTruthy();
  });
});
