import { RequestVariousActivityAcademicTypePipe } from './request-various-activity-academic-type.pipe';

describe('RequestVariousActivityAcademicTypePipe', () => {
  it('create an instance', () => {
    const pipe = new RequestVariousActivityAcademicTypePipe();
    expect(pipe).toBeTruthy();
  });
});
